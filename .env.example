# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# App URL (used for OAuth redirects)
# Local: http://localhost:3000
# Production: https://pixel-github.pages.dev
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API URL for D1/R2 dependent routes
# Local dev: set to production URL to hit deployed D1 database
# Production: leave empty (uses relative URLs)
NEXT_PUBLIC_API_URL=https://pixel-github.pages.dev

# R2 Public URL (for serving uploaded images)
# Set this to your R2 public bucket URL or custom domain
R2_PUBLIC_URL=https://pub-xxx.r2.dev
